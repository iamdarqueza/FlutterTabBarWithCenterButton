import 'package:flutter/material.dart';

class CurrencyPageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Currency'),
        ),
        body: Container(color: Colors.black),
      );
}
